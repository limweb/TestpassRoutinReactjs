var React = require('react');

var NotFound = React.createClass({
    render: function () {
        return (
            <div>
                Hello, SiamHTML NotFound!
            </div>
        );
    }
});

module.exports = NotFound;